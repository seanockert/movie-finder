# TypeSafe API key from console.typesafe.ai/settings/keys
TYPESAFE_API_KEY=sk-...
# TMDB v3 API key from themoviedb.org/settings/api. Only the build step needs it.
# The build step also needs TYPESAFE_API_KEY, for the vibe pass.
TMDB_API_KEY=
PORT=5174
